
kilometerToMeter

function kilometerToMeter(kilometer){
    var meter = kilometer * 1000;
    return meter;
}
var tangailDistance= kilometerToMeter(40);
console.log(tangailDistance);

budgetCalculator

function budgetCalculator(watch, mobile, laptop){
   var result = (watch * 15) + (mobile * 10) + (laptop * 5);
   return result;  
}
var sum = budgetCalculator(50, 100, 500);
console.log(sum);

hotelCost

function hotelCost(firststep, secoundstep, thirdstep){
    var result = (firststep * 10) + (secoundstep * 10) + (thirdstep * 10);
    return result;  
 }
 var sum = hotelCost(100, 80, 50);
 console.log(sum);

megaFriend
 
megaFriend = ['Ruma', 'Sima', 'Salma','Puma'];
